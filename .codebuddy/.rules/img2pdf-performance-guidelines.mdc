---
description: 
alwaysApply: true
enabled: true
updatedAt: 2025-11-11T08:10:16.533Z
provider: 
---

# img2pdf 性能优化指南

## 适用范围
适用于 img2pdf 项目中影响性能的代码部分，特别是图像处理和并行处理模块。

## 规则条款

### 1. 并行处理
- 最大并发数不得超过 8 或 CPU 核心数（取较小值）
- 必须使用 `ProcessPoolExecutor` 进行并行处理
- 每个并行任务必须独立，避免共享状态
- 必须正确处理并行任务的异常和结果

### 2. 图像处理优化
- OCR 检测必须优先使用内存中的图像，避免重复打开文件
- 图像旋转操作必须使用 `expand=True` 参数保持完整图像
- 图像格式转换必须最小化，仅在必要时进行
- 大图像处理前必须考虑内存使用情况

### 3. 内存管理
- 处理完的图像资源必须立即释放
- 临时文件必须在不再需要时立即删除
- 避免在内存中同时保存大量图像数据
- 使用生成器处理大量文件列表

### 4. I/O 优化
- 文件操作必须使用上下文管理器
- 批量操作时必须减少磁盘 I/O 次数
- 临时文件必须创建在目标目录中，避免跨磁盘操作
- 必须使用 `os.replace()` 而非 `os.rename()` 进行文件替换

## 正例

```python
# 并行处理
max_workers = min(os.cpu_count() or 1, 8)
with ProcessPoolExecutor(max_workers=max_workers) as executor:
    future_to_dir = {executor.submit(process_one_dir, t): t[0] for t in tasks}
    for future in as_completed(future_to_dir):
        # 处理结果

# OCR 优化
rot = detect_ocr_rotation(im)
if rot is None and pytesseract is not None:
    rot = detect_rotation_ocr(img_path)

# 内存管理
with BytesIO() as bio:
    im.save(bio, format="JPEG", quality=85)
    bio.seek(0)
    ir = ImageReader(bio)
    c.drawImage(ir, x, y, new_w, new_h, preserveAspectRatio=True)
    c.showPage()
```

## 反例

```python
# 错误的并行处理
max_workers = 16  # 超过推荐值
for task in tasks:
    process_one_dir(task)  # 串行处理

# 错误的 OCR 处理
rot = detect_rotation_ocr(img_path)  # 总是重新打开文件
if rot is None:
    rot = detect_ocr_rotation(im)

# 错误的内存管理
bio = BytesIO()
im.save(bio, format="JPEG")
bio.seek(0)
ir = ImageReader(bio)
c.drawImage(ir, x, y, new_w, new_h, preserveAspectRatio=True)
c.showPage()
bio.close()  # 可能导致内存泄露
```

## 落地检查清单
- [ ] 并行处理是否使用了正确的最大并发数
- [ ] OCR 检测是否优先使用内存中的图像
- [ ] 所有图像资源是否使用上下文管理器
- [ ] 临时文件是否在目标目录创建
- [ ] 是否使用 `os.replace()` 进行文件替换
- [ ] 是否避免了不必要的图像格式转换

## 关联参考
- Python 并发编程文档：https://docs.python.org/3/library/concurrent.futures.html
- Pillow 性能指南：https://pillow.readthedocs.io/en/stable/handbook/concepts.html#performance