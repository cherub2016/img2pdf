---
description: 
alwaysApply: false
enabled: true
updatedAt: 2025-11-11T08:10:50.326Z
provider: 
---

# img2pdf 测试指南

## 适用范围
适用于 img2df 项目的所有测试活动，包括单元测试、集成测试和性能测试。

## 规则条款

### 1. 测试覆盖范围
- 必须测试所有核心功能模块
- 必须测试所有错误处理路径
- 必须测试不同图像格式的处理
- 必须测试并行处理功能
- 必须测试临时文件和资源管理

### 2. 测试数据准备
- 必须包含各种方向的测试图像（0°, 90°, 180°, 270°）
- 必须包含不同格式的测试图像（JPG, PNG, BMP, TIFF）
- 必须包含无文字的测试图像（测试 OCR 降级）
- 必须包含损坏的测试图像（测试错误处理）
- 必须包含大尺寸图像（测试性能）

### 3. 测试环境要求
- 必须在有/无 Tesseract 环境下测试
- 必须在有/无 Ghostscript 环境下测试
- 必须在不同操作系统下测试
- 必须测试不同权限级别的目录

### 4. 测试执行规范
- 单元测试必须独立运行，不依赖外部文件
- 集成测试必须使用临时目录
- 性能测试必须测量处理时间和内存使用
- 必须验证输出 PDF 的完整性和正确性

### 5. 测试报告要求
- 必须记录测试覆盖率
- 必须记录性能基准数据
- 必须记录所有失败测试的详细信息
- 必须提供测试环境配置信息

## 正例

```python
# 单元测试示例
def test_correct_exif_orientation():
    # 创建测试图像
    with Image.new("RGB", (100, 100)) as im:
        # 添加 EXIF 方向信息
        # 测试不同方向值
        for orientation in [1, 3, 6, 8]:
            result = correct_exif_orientation(im)
            assert result is not None

# 集成测试示例
def test_make_pdf_from_images(tmp_path):
    # 准备测试图像
    img_paths = prepare_test_images(tmp_path)
    output_path = tmp_path / "test.pdf"
    
    # 执行测试
    result = make_pdf_from_images(img_paths, str(output_path))
    
    # 验证结果
    assert result is True
    assert output_path.exists()
    assert output_path.stat().st_size > 0

# 错误处理测试
def test_invalid_image_handling():
    with pytest.raises(Exception):
        with Image.open("nonexistent.jpg"):
            pass
```

## 反例

```python
# 错误的测试设计
def test_processing():
    # 依赖外部文件，不可重复
    process_images("/path/to/user/images")
    
# 错误的测试数据
def test_with_single_image():
    # 只测试一种情况，覆盖不足
    process_single_test_image()
    
# 错误的测试验证
def test_output():
    # 未验证输出正确性
    generate_pdf()
    assert True  # 无意义的断言
```

## 落地检查清单
- [ ] 是否测试了所有核心功能模块
- [ ] 是否测试了所有错误处理路径
- [ ] 是否准备了全面的测试数据
- [ ] 是否在不同环境下进行了测试
- [ ] 是否验证了输出 PDF 的正确性
- [ ] 是否记录了测试覆盖率和性能数据

## 关联参考
- pytest 文档：https://docs.pytest.org/
- Python 测试最佳实践：https://docs.python.org/3/library/unittest.html