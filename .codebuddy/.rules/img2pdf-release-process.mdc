---
description: 
alwaysApply: false
enabled: true
updatedAt: 2025-11-11T08:11:05.151Z
provider: 
---

# img2pdf 发布流程规范

## 适用范围
适用于 img2pdf 项目的所有版本发布活动，包括版本规划、代码审查、测试验证和发布准备。

## 规则条款

### 1. 版本规划
- 每个版本必须有明确的版本号（v0.xx 格式）
- 每个版本必须有详细的功能更新说明
- 必须维护版本更新日志（CHANGELOG）
- 重大更新必须提前规划并通知用户

### 2. 代码审查
- 所有代码更改必须经过代码审查
- 必须检查是否符合项目编码规范
- 必须验证是否影响现有功能
- 必须确认是否更新了相关文档

### 3. 测试验证
- 发布前必须执行完整的测试套件
- 必须验证所有支持的图像格式
- 必须测试在不同环境下的兼容性
- 必须进行性能回归测试

### 4. 文档更新
- 必须更新 README.md 中的版本信息
- 必须更新代码中的版本号注释
- 必须更新依赖项版本要求
- 必须更新使用示例和说明

### 5. 发布准备
- 必须创建发布标签（Git tag）
- 必须生成发布说明（Release notes）
- 必准备完整的安装包
- 必须验证安装包的完整性

## 正例

```markdown
# 版本更新日志示例
## v0.43 (2023-11-11)
### 新增功能
- 支持 PNG, BMP, TIFF 图像格式
- 改进资源管理和错误处理

### 优化改进
- 优化 OCR 检测逻辑
- 改进临时文件处理
- 增强输入验证

### 问题修复
- 修复内存泄露问题
- 修复异常处理不当问题
```

```bash
# 发布命令示例
git tag -a v0.43 -m "Release version 0.43"
git push origin v0.43
python setup.py sdist bdist_wheel
twine upload dist/*
```

## 反例

```bash
# 错误的发布流程
git add .
git commit -m "update"
git push  # 缺少版本标签和发布说明
```

## 落地检查清单
- [ ] 版本号是否已更新
- [ ] 功能更新说明是否完整
- [ ] 代码审查是否已完成
- [ ] 所有测试是否通过
- [ ] 文档是否已更新
- [ ] 发布标签是否已创建
- [ ] 发布说明是否已准备
- [ ] 安装包是否已生成和验证

## 关联参考
- 语义化版本规范：https://semver.org/
- Python 打包发布指南：https://packaging.python.org/