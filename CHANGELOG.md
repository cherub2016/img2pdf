# 更新日志 (Changelog)

## [v0.46] - 2025-11-18

### ✨ 新功能 (Features)
- **智能跳过**: 处理前检查目标PDF是否已存在，避免重复处理
  - 自动检测输出目录中的同名PDF文件
  - 跳过已处理的文件夹，节省时间和资源
  - 提供清晰的日志提示

### 📝 文档 (Documentation)
- 移除代码文件中的更新日志，统一在 CHANGELOG.md 中维护
- 更新版本号至 v0.46
- 优化文档结构和可读性

---

## [v0.45] - 2025-11-18

### 🔧 修复 (Fixed)
- **API 兼容性**: 将已弃用的 `_getexif()` 方法替换为现代 Pillow API `getexif()`
  - 使用 `0x0112` 直接获取 Orientation 标签
  - 提高向前兼容性，避免未来版本弃用警告
  
### ⚡ 优化 (Optimized)
- **代码简化**: 移除冗余的 `detect_rotation_ocr()` 函数
  - 统一使用 `detect_ocr_rotation()` 进行 OCR 方向检测
  - 消除了不必要的双重文件打开操作
  - 减少约 30 行冗余代码
  
- **OCR 检测改进**:
  - 统一返回值为整数 `0`，替代之前的 `None`
  - 简化调用逻辑，无需额外的 None 检查
  - 添加图像格式自动转换，提高 OCR 准确性

### 🛡️ 安全 (Security)
- **路径验证增强**: Ghostscript PDF/A 转换功能添加安全检查
  - 验证输入 PDF 文件存在性
  - 使用 `os.path.abspath()` 规范化路径，防止路径遍历攻击
  - 增强文件操作安全性

### 📝 文档 (Documentation)
- 更新版本号至 v0.45
- 在文档字符串中添加完整更新日志
- 优化代码注释，提高可读性

---

## [v0.44] - 之前版本

### 功能特性
- EXIF Orientation 优先处理
- Tesseract OCR 方向检测兜底
- 并行处理多个子文件夹
- 自然排序文件名
- 可选 PDF/A-1b 转换（Ghostscript）
- 支持多种图像格式（PNG, BMP, TIFF, JPEG）
- 资源管理和错误处理
- 运行时间统计

---

## 贡献指南

如需报告问题或提交改进建议，请遵循以下格式：
- **Bug 报告**: 包含复现步骤、错误信息、系统环境
- **功能请求**: 描述用例、预期行为、可能的实现方案
